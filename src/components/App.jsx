import React from 'react';
import { selectedSong } from '../Actions';
import SongsList from './SongsList';

export default () => {
    
    return (
        <div>
            <SongsList />
        </div>
    );
};